;; Hand-port backlog from bb.edn (ADR-2607173000).
{:test {:kind :complex, :doc "Run the multiformats.core test suite (ipfs-parity vectors + round-trips; no network).", :form "(do (require 'clojure.test 'multiformats.core-test) (let [r (clojure.test/run-tests 'multiformats.core-test)] (when (pos? (+ (:fail r) (:error r))) (System/exit 1))))"}}
